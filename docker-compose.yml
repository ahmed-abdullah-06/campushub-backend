services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://host.docker.internal:27017/campushub_db
      - JWT_SECRET=super_secret_jwt_key_campushub_2026
      - CLIENT_URL=http://localhost:5173
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always